version: 1.0
# This should match the app - package name in your setup.py
name: traquitanas

requirements:
  # Putting in a skip true param will skip the entire section. Ignoring the option will assume it be set to False
  skip: true # false Michel
  conda:
    channels:
      - rfun
      - tethysplatform
    packages:

  pip:

post:
  